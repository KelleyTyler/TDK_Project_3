<!DOCTYPE html>
<html>
  <head>
    <title>TDKProject3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <h1><b>TYLER KELLEY PROJECT 3</b></h1>
    <%= button_to 'LOGOUT', '/logout'%>
    <%= link_to 'SHOW CARD DECKS ', '/deck/show/'+cookies[:user]["id"] %>
    <%= link_to 'CREATE CARD DECKS', '/deck/create_deck/'%>
    <%#= link_to 'CREATE CARDS ','/card/makeCard'%>
    <%= yield %>
  </body>
</html>

<script>
const testingCookie = () =>{
  console.log("cookieTest:")
  if(localStorage.getItem("user")===null)
  {
  console.log("CLEAR")
  }
  else
  {
    console.log(localStorage.getItem("user"));
  }
}
const simplifiedLogout = () =>
{
  if(!(localStorage.getItem("user")===null))
  {
    localStorage.removeItem("user");
  }
}
const getFromCookie= () =>
{

}
</script>